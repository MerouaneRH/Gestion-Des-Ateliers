{% extends 'base.html.twig' %}

{% block title %}Create New Atelier{% endblock %}

{% block body %}
    {{ include('atelier/_navbar.html.twig') }}

    <div class="container mt-5">
        <h1 class="mb-4">Create New Atelier</h1>

        <!-- Formulaire de création -->
        <div class="card">
            <div class="card-body">
                {{ include('atelier/_form.html.twig') }}
            </div>
        </div>

        <!-- Lien vers la liste -->
        <div class="mt-4">
            <a href="{{ path('app_atelier_index') }}" class="btn btn-secondary">Back to list</a>
        </div>
    </div>
{% endblock %}
