{% extends 'base.html.twig' %}

{% block title %}Edit Atelier{% endblock %}

{% block body %}
    {{ include('atelier/_navbar.html.twig') }}

    <div class="container mt-5">
        <h1 class="mb-4">Edit Atelier</h1>

        <!-- Edit form card -->
        <div class="card">
            <div class="card-body">
                {{ include('atelier/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
        </div>

        <!-- Links to navigate -->
        <div class="mt-4">
            <a href="{{ path('app_atelier_index') }}" class="btn btn-primary">Retour à la liste</a>
        </div>

        <!-- Delete form -->
        <div class="mt-4">
            {{ include('atelier/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
