<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <!-- Navbar brand (logo) -->
        <a class="navbar-brand" href="{{ path('app_atelier_index') }}">
            <i class="bi-house" style="font-size: 1.5rem; color: cornflowerblue;"></i>
            <span class="ms-2 text-white">Accueil</span>
        </a>

        <!-- Navbar links -->
        <div class="d-flex justify-content-end align-items-center">
            <a class="btn btn-outline-light me-2" href="{{ path('app_atelier_new') }}">
                <span class="ms-2">Nouveau</span>
            </a>
        </div>
        <div class="d-flex justify-content-end align-items-center">
            {% if app.user %}
                <a href="{{ path('app_logout') }}">Se deconnecter</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-primary">Se connecter</a>
            {% endif %}
        </div>
    </div>
</nav>

<!-- Bootstrap Icons CDN -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">